# frontend/shared

该目录用于存放用户端与后台端共用的前端基础能力，不承载业务页面。

当前约定：

- `constants`
  - 共用状态枚举、状态标签、基础常量
- `types`
  - 共用 JSDoc 类型定义与字段键名约定
- `utils`
  - 请求核心、查询串工具、纯工具函数

放入条件：

- 同时被 `frontend/app` 与 `frontend/admin` 使用
- 不依赖某一端独有的页面组件或交互
- 不改变业务流程，只提供一致性基础设施
