# 前端缺失页面及功能清单

## 一、完全缺失的页面

### 1. 实名认证审核页面

| 项目 | 详情 |
|---|---|
| **页面名称** | 实名认证审核 |
| **路由路径** | `/admin/realname-audit` |
| **对应组件** | `RealnameAuditView.vue`（需新建） |
| **缺失原因** | 核心业务流程页面完全未创建 |
| **后端接口** | `POST /api/admin/users/:id/manual-check`（已存在） |

**需实现功能**：
- [ ] 实名信息列表页（表格展示：用户ID、真实姓名、身份证号、提交时间、审核状态）
- [ ] 状态筛选栏（待审核 / 已通过 / 已拒绝 / 全部）
- [ ] 身份证照片预览（正反面大图查看）
- [ ] 审核操作抽屉（通过 / 拒绝 + 审核备注输入框）
- [ ] 用户注册信息联动展示（手机号脱敏、注册时间、注册渠道）
- [ ] 批量审核功能（可选）
- [ ] 审核记录查询（按时间、按操作人筛选）

**需新建文件**：
```
frontend/admin/src/views/RealnameAuditView.vue     [新建]
frontend/admin/src/services/realname.js            [新建 - API 封装]
```

---

### 2. 系统配置页面

| 项目 | 详情 |
|---|---|
| **页面名称** | 系统配置 |
| **路由路径** | `/admin/system-config` |
| **对应组件** | `SystemConfigView.vue`（需新建） |
| **缺失原因** | 后续优化需求，尚未开始开发 |
| **后端接口** | 待确认 / 需新增 |

**需实现功能**：
- [ ] 交易时段配置（开盘时间、收盘时间、午休时段设置）
- [ ] 休市规则配置（周末休市开关、节假日休市日期选择器）
- [ ] 费率配置（充值手续费率、提现手续费率、交易佣金率）
- [ ] 提现规则（最低提现金额、单日提现上限、提现到账时间说明）
- [ ] 注册限制（同一IP注册上限、设备指纹限制）
- [ ] 全局公告编辑（富文本编辑器）
- [ ] 配置变更日志（记录谁改了什么、旧值/新值对比）

**需新建文件**：
```
frontend/admin/src/views/SystemConfigView.vue      [新建]
frontend/admin/src/services/config.js              [新建 - API 封装]
```

---

### 3. 数据归档管理页面

| 项目 | 详情 |
|---|---|
| **页面名称** | 数据归档管理 |
| **路由路径** | `/admin/data-archive` |
| **对应组件** | `DataArchiveView.vue`（需新建） |
| **缺失原因** | 合规需求，尚未开始开发 |
| **后端接口** | 待确认 / 需新增 |

**需实现功能**：
- [ ] 归档策略配置（交易流水保留年限、日志保留年限、用户数据保留年限）
- [ ] 手动归档触发按钮（选择时间范围执行归档）
- [ ] 归档任务列表（任务状态、执行时间、归档数据量、操作人）
- [ ] 归档记录查询（按表名、按时间范围检索）
- [ ] 归档数据预览（抽样查看已归档数据内容）
- [ ] 归档数据导出（CSV / Excel 格式）

**需新建文件**：
```
frontend/admin/src/views/DataArchiveView.vue       [新建]
frontend/admin/src/services/archive.js             [新建 - API 封装]
```

---

### 4. 平台资金总览页面

| 项目 | 详情 |
|---|---|
| **页面名称** | 平台资金总览 |
| **路由路径** | `/admin/platform-funds` |
| **对应组件** | `PlatformFundsView.vue`（需新建） |
| **缺失原因** | 托管体系需求，尚未开始开发 |
| **后端接口** | 待确认 / 需新增 |

**需实现功能**：
- [ ] 平台资金大盘（总充值金额、总提现金额、平台当前余额、冻结金额）
- [ ] 实时资金流动图（今日充值 / 提现趋势折线图）
- [ ] 用户资产汇总（总用户数、总持仓市值、总可用余额）
- [ ] 资金池健康度指标（流动性比率、风险准备金比例）
- [ ] 渠道资金分布（支付宝 / 微信 / 银行卡各渠道余额）
- [ ] 异常资金预警（大额异动提醒阈值设置）

**需新建文件**：
```
frontend/admin/src/views/PlatformFundsView.vue     [新建]
frontend/admin/src/services/platform.js            [新建 - API 封装]
```

---

## 二、已有页面但功能缺失

### 5. FundsView.vue — 资金管理页面

| 项目 | 详情 |
|---|---|
| **文件路径** | `src/views/FundsView.vue` |
| **当前状态** | 页面存在，但核心操作功能缺失 |

#### 5.1 充值管理 — 缺失功能

| 缺失功能 | 优先级 | 说明 |
|---|---|---|
| 充值订单详情弹窗 | P1 | 点击行展开/弹窗，显示完整订单信息 |
| 用户手机号脱敏展示 | P1 | 显示后四位，如 `138****5678` |
| 对账操作按钮 | P1 | 标记为"已对账" / "异常" |
| 充值渠道管理 | P2 | 上传/管理收款二维码、银行卡信息 |
| 充值统计卡片 | P2 | 今日充值总额、待对账笔数 |
| 充值退款操作 | P3 | 异常订单退款按钮 |

**需修改位置**：`FundsView.vue` L211-241（充值列表区域）

#### 5.2 提现管理 — 缺失功能

| 缺失功能 | 优先级 | 说明 |
|---|---|---|
| 提现详情抽屉 | P0 | 点击行展开，显示：提现金额、用户手机号后四位、收款账户、申请时间 |
| 审核通过按钮 | P0 | 调用 `approveWithdrawal` 接口 |
| 审核拒绝按钮 | P0 | 调用 `rejectWithdrawal` 接口，需填写拒绝原因 |
| 标记转账完成 | P0 | 调用 `confirmWithdrawalCompleted` 接口 |
| 提现静音/提醒控制 | P1 | 开关：是否发送提现到账短信/推送 |
| 批量审核功能 | P2 | 多选行后批量通过/拒绝 |
| 提现统计卡片 | P2 | 待审核笔数、今日提现总额、审核中金额 |
| 提现渠道筛选 | P2 | 按支付宝/微信/银行卡筛选 |

**需修改位置**：`FundsView.vue` L244-280（提现列表区域）

**需新增 API 封装**（`src/services/admin.js` 或新建 `src/services/fund.js`）：
```javascript
// 需补充的 API 方法
export const approveWithdrawal = (id) => api.post(`/admin/funds/withdrawals/${id}/approve`)
export const rejectWithdrawal = (id, reason) => api.post(`/admin/funds/withdrawals/${id}/reject`, { reason })
export const confirmWithdrawal = (id) => api.post(`/admin/funds/withdrawals/${id}/confirm`)
```

---

### 6. UsersView.vue — 用户管理页面

| 项目 | 详情 |
|---|---|
| **文件路径** | `src/views/UsersView.vue` |
| **当前状态** | 页面存在，用户详情抽屉信息不完整 |

#### 6.1 用户详情抽屉 — 缺失信息板块

| 缺失板块 | 优先级 | 需展示内容 |
|---|---|---|
| 注册信息 | P1 | 注册时间、注册渠道（H5/APP/小程序）、注册IP、设备型号 |
| 手机号脱敏 | P1 | `138****5678` 格式，点击可查看完整（需二次验证） |
| 实名信息 | P1 | 真实姓名、身份证号（脱敏）、实名状态、认证时间 |
| 收款方式 | P1 | 已绑定的微信/支付宝/银行卡（脱敏展示） |
| 资产明细 | P2 | 可用余额、冻结金额、持仓市值、累计盈亏 |
| 登录设备 | P2 | 最近登录设备列表（设备名、IP、时间、操作：踢下线） |
| 操作记录 | P2 | 最近操作日志（登录、修改密码、绑定手机等） |
| 账户状态控制 | P2 | 启用/禁用账户、重置密码、强制登出 |

**需修改位置**：`UsersView.vue` L246-258（用户详情抽屉区域）

---

### 7. TradesView.vue — 交易管理页面

| 项目 | 详情 |
|---|---|
| **文件路径** | `src/views/TradesView.vue` |
| **当前状态** | 页面存在，停盘/恢复操作未对接接口 |

#### 7.1 缺失功能

| 缺失功能 | 优先级 | 说明 |
|---|---|---|
| 全站停盘接口对接 | P2 | 点击"全站停盘"后调用 `pauseTrading` 接口，而非仅显示消息 |
| 恢复交易接口对接 | P2 | 点击"恢复交易"后调用 `resumeTrading` 接口 |
| 停盘原因输入 | P2 | 弹窗中增加原因输入框，同步记录到操作日志 |
| 停盘状态实时显示 | P2 | 页面顶部显示当前交易状态（正常/停盘中） |
| 交易流水导出 | P3 | 导出选中/全部交易记录为 Excel |

**需修改位置**：`TradesView.vue` L65-73（停盘/恢复操作区域）

**需补充的 API 调用**：
```javascript
// 当前代码（仅记录，不调用）
actionMessage.value = '已记录全站停盘操作...本期不直接调用停盘接口'

// 应改为
await api.post('/admin/risk/pause-trading', { reason: pauseReason.value })
```

---

### 8. RiskView.vue — 风控管理页面

| 项目 | 详情 |
|---|---|
| **文件路径** | `src/views/RiskView.vue` |
| **当前状态** | 页面存在，规则更新未对接接口 |

#### 8.1 缺失功能

| 缺失功能 | 优先级 | 说明 |
|---|---|---|
| 风控规则更新接口对接 | P2 | 点击"更新规则"后调用 `updateRules` 接口 |
| 规则版本对比 | P2 | 显示当前规则 vs 新规则的差异对比 |
| 规则回滚功能 | P2 | 支持回滚到上一版本规则 |
| 规则生效时间设置 | P3 | 立即生效 / 定时生效（选择日期时间） |
| 规则变更审批流 | P3 | 重大规则变更需二次确认/审批 |

**需修改位置**：`RiskView.vue` L115-119（规则更新操作区域）

---

### 9. LeaderboardView.vue — 排行榜治理页面

| 项目 | 详情 |
|---|---|
| **文件路径** | `src/views/LeaderboardView.vue` |
| **当前状态** | 页面存在，规则更新/重建未对接接口 |

#### 9.1 缺失功能

| 缺失功能 | 优先级 | 说明 |
|---|---|---|
| 排序规则更新接口对接 | P2 | 点击"更新规则"后调用实际接口 |
| 排行榜重建接口对接 | P2 | 点击"重建榜单"后调用实际接口 |
| 规则参数配置 | P2 | 排行周期（日/周/月）、权重设置、上榜门槛 |
| 榜单数据预览 | P2 | 更新规则前先预览新规则下的榜单结果 |
| 作弊用户标记 | P3 | 手动将用户从榜单移除/标记异常 |

**需修改位置**：`LeaderboardView.vue` L82-96（规则更新/重建操作区域）

---

## 三、路由配置缺失

### 当前路由文件：`src/router/index.js`

**需补充的路由条目**：

```javascript
{
  path: 'realname-audit',
  name: 'realname-audit',
  component: () => import('@/views/RealnameAuditView.vue'),
  meta: { title: '实名认证审核', icon: 'idcard' }
},
{
  path: 'system-config',
  name: 'system-config',
  component: () => import('@/views/SystemConfigView.vue'),
  meta: { title: '系统配置', icon: 'setting' }
},
{
  path: 'data-archive',
  name: 'data-archive',
  component: () => import('@/views/DataArchiveView.vue'),
  meta: { title: '数据归档', icon: 'database' }
},
{
  path: 'platform-funds',
  name: 'platform-funds',
  component: () => import('@/views/PlatformFundsView.vue'),
  meta: { title: '平台资金', icon: 'bank' }
}
```

**需补充的侧边栏菜单项**（通常在布局组件中配置）：
- 实名认证审核
- 平台资金总览
- 系统配置
- 数据归档管理

---

## 四、API 服务层缺失

### 当前已有服务文件
- `src/services/admin.js` — 通用 admin API
- `src/services/auth.js` — 认证相关 API

### 需新建/补充的服务文件

| 文件路径 | 用途 | 优先级 |
|---|---|---|
| `src/services/realname.js` | 实名认证审核相关 API | P0 |
| `src/services/fund.js` | 充值/提现审核 API（补充现有） | P0 |
| `src/services/config.js` | 系统配置 API | P3 |
| `src/services/archive.js` | 数据归档 API | P3 |
| `src/services/platform.js` | 平台资金 API | P3 |

---

## 五、组件层缺失（可复用组件）

以下组件建议在 `src/components/` 下新建，供多个页面复用：

| 组件名 | 用途 | 使用页面 |
|---|---|---|
| `PhoneMask.vue` | 手机号脱敏展示（138****5678），点击解锁 | UsersView, FundsView, RealnameAuditView |
| `IdCardMask.vue` | 身份证号脱敏展示（110**********1234） | RealnameAuditView, UsersView |
| `AuditActionBar.vue` | 审核操作按钮组（通过/拒绝/备注） | RealnameAuditView, FundsView |
| `ImagePreview.vue` | 身份证/资质图片预览（放大/旋转/下载） | RealnameAuditView |
| `DetailDrawer.vue` | 通用详情抽屉容器（可复用布局） | UsersView, FundsView |
| `StatusTag.vue` | 状态标签（待审核/已通过/已拒绝/异常） | 多个页面 |
| `DataTable.vue` | 增强表格（筛选/排序/分页/导出） | 多个页面 |

---

## 六、缺失功能优先级总表

| 优先级 | 页面/功能 | 工作量估算 | 阻塞点 |
|---|---|---|---|
| **P0** | 实名认证审核页面（完整新建） | 3-4 天 | 需确认身份证图片存储方式 |
| **P0** | 提现审核操作（FundsView 补充） | 1-2 天 | 后端接口已就绪 |
| **P1** | 充值对账操作（FundsView 补充） | 1 天 | 需确认对账业务逻辑 |
| **P1** | 用户详情抽屉信息补充 | 1-2 天 | 需确认用户数据结构 |
| **P1** | 用户手机号脱敏展示 | 0.5 天 | 可复用组件 |
| **P2** | 交易停盘/恢复接口对接 | 0.5 天 | 后端接口已就绪 |
| **P2** | 风控规则更新接口对接 | 0.5 天 | 后端接口已就绪 |
| **P2** | 排行榜治理接口对接 | 0.5 天 | 需确认接口参数 |
| **P3** | 系统配置页面（完整新建） | 2-3 天 | 需后端提供配置接口 |
| **P3** | 平台资金总览页面（完整新建） | 2-3 天 | 需后端提供统计数据接口 |
| **P3** | 数据归档页面（完整新建） | 2-3 天 | 需后端提供归档接口 |

---

## 七、开发顺序建议

```
第一阶段（本周）:
  ├─ 1. 提现审核操作（FundsView 补充）
  ├─ 2. 用户手机号脱敏展示（复用组件）
  └─ 3. 用户详情抽屉信息补充

第二阶段（下周）:
  ├─ 4. 实名认证审核页面（完整新建）
  ├─ 5. 充值对账操作（FundsView 补充）
  └─ 6. 通用组件封装（PhoneMask, AuditActionBar 等）

第三阶段:
  ├─ 7. 交易停盘/恢复接口对接
  ├─ 8. 风控规则更新接口对接
  └─ 9. 排行榜治理接口对接

第四阶段（后续规划）:
  ├─ 10. 系统配置页面
  ├─ 11. 平台资金总览页面
  └─ 12. 数据归档页面
```

---

*文档生成时间：2026-05-13*  
*适用范围：frontend/admin 前端开发团队*
