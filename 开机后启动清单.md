# 金影子开机后启动清单

本文用于 Windows 环境下，每次开机后，从打开 Codex 到把项目页面跑起来的最短流程。

## 日常启动清单

### 1. 先启动基础软件

按顺序打开：

1. Docker Desktop
2. Codex Desktop
3. 一个 PowerShell 窗口

说明：

- 一定要等 Docker Desktop 完全启动完成，再执行后面的命令。
- 如果 Docker 没启动，后端会报数据库连接失败。

### 2. 在 Codex 打开项目

在 Codex 中打开工作目录：

```text
E:\xiangmu\金影子
```

### 3. 在 PowerShell 进入项目根目录

```powershell
cd E:\xiangmu\金影子
```

如果你用的是 `cmd`，要用：

```cmd
cd /d E:\xiangmu\金影子
```

### 4. 启动数据库和 Redis

```powershell
cd .\backend
docker compose up -d
cd ..
```

### 5. 启动后端

在项目根目录执行：

```powershell
npm run dev:backend
```

### 6. 启动后台前端

再开一个 PowerShell 窗口，进入根目录执行：

```powershell
cd E:\xiangmu\金影子
npm run dev:admin
```

### 7. 如果要看 App 端页面，再启动用户端前端

再开一个 PowerShell 窗口，进入根目录执行：

```powershell
cd E:\xiangmu\金影子
npm run dev:app
```

## 页面访问地址

默认常用地址：

- 后端 API: `http://localhost:3001/api`
- Swagger: `http://localhost:3001/docs`
- 后台前端: 以 `npm run dev:admin` 控制台实际输出地址为准
- App 前端: 以 `npm run dev:app` 控制台实际输出地址为准

## 每天最短版

平时如果环境没变化，只需要记这几步：

```powershell
cd E:\xiangmu\金影子\backend
docker compose up -d

cd E:\xiangmu\金影子
npm run dev:backend
```

再分别开新窗口执行：

```powershell
cd E:\xiangmu\金影子
npm run dev:admin
```

```powershell
cd E:\xiangmu\金影子
npm run dev:app
```

## 首次启动或环境异常时补做

只有首次拉代码、依赖变动、Prisma 变动、数据库重建时，才需要额外执行下面这些命令。

### 1. 安装依赖

根目录：

```powershell
cd E:\xiangmu\金影子
npm install
```

### 2. 生成 Prisma Client

```powershell
cd E:\xiangmu\金影子\backend
npm run prisma:generate
```

### 3. 执行数据库迁移

```powershell
cd E:\xiangmu\金影子\backend
npx prisma migrate deploy
```

## 常见问题

### 1. `Missing script: "dev"`

原因：

- `backend` 没有 `npm run dev`

正确命令：

```powershell
npm run start:dev
```

或者在项目根目录执行：

```powershell
npm run dev:backend
```

### 2. `Can't reach database server at localhost:5432`

原因：

- PostgreSQL 没启动
- Docker Desktop 没启动

处理：

```powershell
cd E:\xiangmu\金影子\backend
docker compose up -d
```

### 3. `no configuration file provided: not found`

原因：

- 你不在 `backend` 目录里执行 `docker compose`

正确方式：

```powershell
cd E:\xiangmu\金影子\backend
docker compose up -d
```

### 4. `npm ERR! enoent Could not read package.json`

原因：

- 你当前目录不对

先确认当前目录，再执行命令。

### 5. `docker ... daemon is running` 相关报错

原因：

- Docker Desktop 没完全启动

处理：

- 先打开 Docker Desktop
- 等状态正常后再重试

## 关闭环境

如果当天开发结束，常用关闭方式如下。

前端和后端运行窗口里按：

```text
Ctrl + C
```

如果还要停掉数据库和 Redis：

```powershell
cd E:\xiangmu\金影子\backend
docker compose down
```

